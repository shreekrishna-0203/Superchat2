<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="superchat" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="Dialog">
        <item name="android:windowBackground">@drawable/empty_background</item>
        <item name="windowNoTitle">true</item>
        <item name="windowMinWidthMajor">80%</item>
        <item name="windowActionBar">false</item>
    </style>

    <!-- Dark mode specific styles -->
    <style name="superchatDark" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize dark mode text color here -->
        <item name="android:textColor">@android:color/white</item>
    </style>


    <color name="textColor">@android:color/white</color>


</resources>
